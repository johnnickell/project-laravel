# Contributing

Create a feature branch from `develop`, keep changes within a local ticket, and run `./bin/build` before opening a pull request. Run `./bin/planning-check` after changing planning files. Do not add copied Fight Common or Fight AccessControl source; Laravel-specific composition belongs in this repository.