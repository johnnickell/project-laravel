#!/usr/bin/env sh
set -eu

mkdir -p var/cache/laravel var/cache/phpunit
# PHPUnit's explicit test environment must not inherit Laravel's development config cache.
docker compose run --rm api env -u APP_CONFIG_CACHE php vendor/bin/phpunit "$@"
